<Application x:Class="AimPointGenerator.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             StartupUri="MainWindow.xaml"
             Startup="App_Startup">
    <Application.Resources>
        <sys:Double x:Key="{x:Static SystemParameters.VerticalScrollBarWidthKey}">8</sys:Double>
        <sys:Double x:Key="{x:Static SystemParameters.HorizontalScrollBarHeightKey}">8</sys:Double>
        <!-- 테마 리소스 (ThemeManager가 런타임에 갱신) -->
        <SolidColorBrush x:Key="ThemeWindowBackground" Color="#1a2a1a"/>
        <SolidColorBrush x:Key="ThemePanelBackground" Color="#0d1f0d"/>
        <SolidColorBrush x:Key="ThemePanelInner" Color="#0a150a"/>
        <SolidColorBrush x:Key="ThemeTitleForeground" Color="#90EE90"/>
        <SolidColorBrush x:Key="ThemeLabelForeground" Color="#90EE90"/>
        <SolidColorBrush x:Key="ThemeSectionHeaderForeground" Color="#7CCD7C"/>
        <SolidColorBrush x:Key="ThemeTextBoxBackground" Color="#0d1f0d"/>
        <SolidColorBrush x:Key="ThemeTextBoxForeground" Color="#90EE90"/>
        <SolidColorBrush x:Key="ThemeTextBoxBorder" Color="#2d5a2d"/>
        <SolidColorBrush x:Key="ThemeButtonPrimary" Color="#2d5a2d"/>
        <SolidColorBrush x:Key="ThemeButtonDelete" Color="#5a2020"/>
        <SolidColorBrush x:Key="ThemeBorderColor" Color="#2d5a2d"/>
        <SolidColorBrush x:Key="ThemeItemBackground" Color="#152515"/>
        <SolidColorBrush x:Key="ThemeItemDragHandle" Color="#1a3a1a"/>
        <SolidColorBrush x:Key="ThemeStatusForeground" Color="#FF6B6B"/>
        <SolidColorBrush x:Key="ThemeScrollBarBackground" Color="#0d1f0d"/>
        <SolidColorBrush x:Key="ThemeScrollBarThumb" Color="#2d4a2d"/>
        <SolidColorBrush x:Key="ThemeScrollBarThumbHover" Color="#3d5a3d"/>
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="{DynamicResource ThemeButtonPrimary}"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border x:Name="border" CornerRadius="3" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.8"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type TextBox}">
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ScrollBarRepeatButton" TargetType="{x:Type RepeatButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type RepeatButton}">
                        <Border Background="Transparent"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="ThumbBorder" CornerRadius="4"
                                Margin="0" MinHeight="20"
                                Background="{DynamicResource ThemeScrollBarThumb}" BorderBrush="{DynamicResource ThemeItemBackground}" BorderThickness="1"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource ThemeScrollBarThumbHover}"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="ThumbBorder" Property="Background" Value="{DynamicResource ThemeScrollBarThumbHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ThumbBorder" Property="Visibility" Value="Collapsed"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="{x:Type ScrollBar}">
            <Setter Property="Background" Value="{DynamicResource ThemeScrollBarBackground}"/>
            <Setter Property="Width" Value="2"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid>
                            <Border Background="{DynamicResource ThemeScrollBarBackground}" CornerRadius="4"/>
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Style="{StaticResource ScrollBarRepeatButton}"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Style="{StaticResource ScrollBarRepeatButton}"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Application.Resources>
</Application>
